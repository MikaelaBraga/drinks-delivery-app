// implementar algumas refatrações nesse arquivo

// import { useContext, useEffect, useState } from 'react';
// import { CartContext } from '../context/CartProvider';

// function ItemQuantityCheckout() {
//   const [itemQuantity, setItemQuantity] = useState(0);
//   const { cart } = useContext(CartContext);

//   useEffect(() => {
//     console.log(itemQuantity);

// const verifyProductCart = cart.find((c) => c.productId === id);

// if (verifyProductCart) {
//   const getQuantity = cart.find((c) => c.productId === id)?.quantity;
//   setItemQuantity(getQuantity);
// } else {
// }
//   }, [cart, itemQuantity]);

//   return [itemQuantity, setItemQuantity];
// }

// export default ItemQuantityCheckout;
